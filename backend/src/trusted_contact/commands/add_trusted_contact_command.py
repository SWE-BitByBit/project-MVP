class AddTrustedContactCmd:
    def __init__(
        self,
        user_id,
        contact_name,
        contact_email,
        contact_phone_number,
    ):
        self.user_id = user_id,
        self.contact_name = contact_name,
        self.contact_email = contact_email,
        self.contact_phone_number = contact_phone_number,
        